---
description: worker implements, reviewer audits, worker applies feedback
---
Use the `subagent` tool in chain mode:

1. `worker` implements: $@
2. `reviewer` reviews `{previous}`
3. `worker` applies `{previous}` feedback

Return final worker output.
