---
description: scout discovers context, planner returns an implementation plan
---
Use the `subagent` tool in chain mode:

1. Run `scout` for: $@
2. Run `planner` with the previous step output using `{previous}`

Return the planner output.
