---
description: scout -> planner -> worker execution chain
---
Use the `subagent` tool in chain mode:

1. `scout` for: $@
2. `planner` on `{previous}`
3. `worker` to execute `{previous}`

Return the worker output.
